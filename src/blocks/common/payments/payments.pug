mixin payments(data = {}, mods = {})
    ul.payments&attributes(attributes)
        each payment in data
            li.payments__item
                a(href="javascript:void(0);").payments__link
                    img(src=payment.img, alt=payment.title).payments__img